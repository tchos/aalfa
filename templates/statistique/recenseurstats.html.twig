{% extends 'base.html.twig' %}

{% block title %}STATS PAR EQUIPE{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('asset/css/dataTables.dataTables.css') }}">
{% endblock %}

{% block contenttitle %}Statistiques globales des saisies par agent de collecte !{% endblock %}

{% block aside %}
    {% include '_partials/_aside.html.twig' with {
        'cpteurDailyUser': compteurUserJour,
        'cpteurUser': compteurUser,
        'totalSaisie': totalSaisie
    } %}
{% endblock %}

{% block body %}
    {% include '_partials/_globalstat.html.twig' with {'stats': stats } %}
    <div class="row">
        <div class="col-sm-9 mx-auto">
            <table id="recenseurstats" class="display compact" style="width:100%">
                <thead>
                    <tr>
                        <th class="text-start">Rang</th>
                        <th class="text-start">Agent de collecte</th>
                        <th class="text-start">Equipe</th>
                        <th class="text-start">Nombre actes collectés</th>
                    </tr>
                </thead>
                <tbody>
                    {% set i = 1 %}
                    {% for recenseur in recenseurStats %}
                        <tr>
                            <td class="text-start">{{ i }}</td>
                            <td><img src="{% if i == 1 %}
                                                {{ asset('asset/images/medal_gold.png') }}
                                            {% elseif i == 2 %}
                                                {{ asset('asset/images/medal_silver.png') }}
                                            {% elseif i == 3 %}
                                                {{ asset('asset/images/medal_bronze.png') }}
                                            {% endif %}" width="20px" class="pe-1" alt="">
                                {{ recenseur.nom }}
                            </td>
                            <td class="text-start">{{ recenseur.equipe }}</td>
                            <td class="text-start">{{ recenseur.nb_enfant | number_format(0,'.',' ') }}</td>
                        </tr>
                        {% set i = i + 1 %}
                    {% else %}
                        <span class="fw-bolder text-primary">Aucune saisie n'a encore été effectuée ce jour.</span>
                    {% endfor %}
                </tbody>
                <tfoot>
                    <tr>
                        <th class="text-start">Rang</th>
                        <th class="text-start">Agent de collecte</th>
                        <th class="text-start">Equipe</th>
                        <th class="text-start">Nombre actes collectés</th>
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('asset/js/dataTables.bootstrap5.js') }}"></script>
    <script src="{{ asset('asset/js/jquery-3.7.1.js') }}"></script>
    <script src="{{ asset('asset/js/dataTables.js') }}"></script>

    <script src="{{ asset('asset/js/dataTables.buttons.js') }}"></script>
    <script src="{{ asset('asset/js/buttons.dataTables.js') }}"></script>
    <script src="{{ asset('asset/js/jszip.min.js') }}"></script>
    <script src="{{ asset('asset/js/pdfmake.min.js') }}"></script>
    <script src="{{ asset('asset/js/vfs_fonts.js') }}"></script>
    <script src="{{ asset('asset/js/buttons.html5.min.js') }}"></script>
    <script src="{{ asset('asset/js/buttons.print.min.js') }}"></script>
    <script>
        $(document).ready(function(){
            new DataTable('#recenseurstats', {
                layout: {
                    topStart: {
                        pageLength: true,
                        buttons: [
                            {
                                extend: 'copy',
                                text: '<i class="fa fa-copy"></i> Copier',
                                className: 'btn btn-secondary btn-sm'
                            },
                            {
                                extend: 'csv',
                                text: '<i class="fa fa-file-csv"></i> CSV',
                                className: 'btn btn-info btn-sm'
                            },
                            {
                                extend: 'excel',
                                text: '<i class="fa fa-file-excel"></i> Excel',
                                className: 'btn btn-success btn-sm'
                            },
                            {
                                extend: 'pdf',
                                text: '<i class="fa fa-file-pdf"></i> PDF',
                                className: 'btn btn-danger btn-sm',
                                orientation: 'landscape',
                                pageSize: 'A4'
                            },
                            {
                                extend: 'print',
                                text: '<i class="fa fa-print"></i> Imprimer',
                                className: 'btn btn-dark btn-sm'
                            }
                        ]
                    }
                }
            });
        });
    </script>
{% endblock %}